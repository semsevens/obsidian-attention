# 周报

## 本周

- 把 Attention 的存储层和回顾面板搭起来了
- 锚定逻辑单独抽成纯函数，27 个测试
- markdown 宿主：实时预览走 CodeMirror 装饰，阅读模式走 post-processor

## 下周

字幕宿主。需要 media-transcript 那边加一个渲染完成的广播事件。

## 一段中文长文本

反向传播并不需要保存全部中间激活，只要在反向时重新算一遍就行，
这就是 gradient checkpointing 的核心思路，用计算换显存。
选中「反向传播」这四个字划线，测 CJK 下的锚定和高亮渲染。
